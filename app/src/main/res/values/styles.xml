<resources>
    <!-- the theme applied to the application or activity -->
    <style name="CustomActionBarTheme"
        parent="@style/Theme.AppCompat.Light">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/TabTextStyle</item>
        <item name="android:actionMenuTextColor">@color/indigo_700</item>
        <item name="colorControlNormal">@color/indigo_200</item>
        <item name="colorControlActivated">@color/indigo_500</item>
        <item name="colorControlHighlight">#FF0000</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:buttonStyle">@style/button</item>
        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="actionBarTabTextStyle">@style/TabTextStyle</item>
        <item name="actionMenuTextColor">@color/indigo_50</item>
    </style>

    <!-- general styles for the action bar -->
    <style name="MyActionBar"
        parent="Widget.AppCompat.ActionBar">
        <item name="background">@color/indigo_700</item>
        <item name="android:backgroundStacked">@color/indigo_50</item>
        <item name="android:backgroundSplit">@color/indigo_50</item>
        <item name="android:titleTextStyle">@style/TitleTextStyle</item>

        <!-- Support library compatibility -->
        <item name="titleTextStyle">@style/TitleTextStyle</item>
    </style>

    <!-- action bar title text -->
    <style name="TitleTextStyle"
        parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/indigo_50</item>
    </style>

    <!-- action bar tab text -->
    <style name="TabTextStyle"
        parent="@style/Widget.AppCompat.ActionBar.TabText">
        <item name="android:textColor">@color/indigo_50</item>
    </style>
    <style name="button" parent="android:style/Widget.Button">
        <item name="android:colorPressedHighlight">@color/indigo_300</item>
        <item name="android:state_pressed">@color/indigo_300</item>
        <item name="android:state_active">@color/indigo_300</item>
    </style>
</resources>
